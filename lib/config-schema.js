'use babel';

// json-schema.org
module.exports = {
  saveSyntaxConfig: {
    title: 'Save Syntax Configuration',
    description: 'By default, only the configuration for the UI theme is saved. Enable this setting to save UI configuration to the theme.',
    type: 'boolean',
    default: true
  }
};
